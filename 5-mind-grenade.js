const num1 = 5;
const num2 = 10;

function addValue()
{
    console.log(`The sum is : ${num1 + num2}`);
    
}

addValue();

//  whne we export and import a module the node wrap around that module in funtions so when we import it in anohter module it invokes that module.